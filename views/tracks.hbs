<div>
  <table>
    <tr>
      <th>title</th>
      <th>track</th>
    </tr>
    {{#each tracks}}
    <tr>
      <td>{{name}}</td>
     <td>
      <audio controls>
 <source src="{{preview_url}}" type="audio/ogg; codecs=opus"/>
 <source src="{{preview_url}}" type="audio/ogg; codecs=vorbis"/>
 <source src="{{preview_url}}" type="audio/mpeg"/>
</audio>
     
     
     
     </td>

    </tr>
  {{/each}}
  </table>

</div>