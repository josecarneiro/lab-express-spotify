<div class="d-flex flex-row justify-content-between align-items-center bg-light m-1">
    <p class="font-weight-bold">{{this.name}}</p>
    <audio controls>
        <source src={{this.preview_url}} />
    </audio>
    <!--<audio src="{{this.preview_url}}">Music</audio>-->
</div>